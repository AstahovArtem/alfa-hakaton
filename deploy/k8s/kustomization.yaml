apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: pdn
resources:
  - configmap.yaml
  - deployment.yaml
  - service.yaml
  - hpa.yaml
  - ingress.yaml
  - pdb.yaml
images:
  - name: pdn-shield
    newTag: latest